import 'package:flutter/material.dart';

class AboutUsSession extends StatelessWidget {
  const AboutUsSession({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 900,
      child: Placeholder(
        color: Colors.black,
      ),
    );
  }
}
